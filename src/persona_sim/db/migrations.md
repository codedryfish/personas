# Database migrations

For the MVP, schema migrations are managed manually. Apply SQL changes directly to the
database as needed and keep the SQL scripts versioned alongside application changes once
the migration story evolves.
